# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Check Liste' do
	pod 'M13Checkbox'
	pod 'Firebase/Core'
	pod 'Firebase/Firestore'
	pod 'Firebase'
	pod 'Firebase/Database'
	pod 'Firebase/Auth'
	pod 'Floaty'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Check Liste

  target 'Check ListeTests' do
    inherit! :search_paths
	pod 'Firebase'
    # Pods for testing
  end

  target 'Check ListeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
